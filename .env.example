# Copy to `.env` and fill in to enable real contact-form delivery.
# Any endpoint that accepts a JSON POST works (Formspree, EmailJS proxy,
# Resend proxy, your own API route). Leave unset for local "demo" mode.
VITE_CONTACT_ENDPOINT=
